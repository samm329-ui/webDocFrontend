// This file is used to store the Firebase configuration.
// It is important to keep this file in a safe place and not to
// expose the configuration to the public.
// The configuration is used to initialize the Firebase app.
export const firebaseConfig = {
  apiKey: 'your-api-key',
  authDomain: 'your-auth-domain',
  projectId: 'your-project-id',
  storageBucket: 'your-storage-bucket',
  messagingSenderId: 'your-messaging-sender-id',
  appId: 'your-app-id',
};
